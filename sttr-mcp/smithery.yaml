name: sttr-mcp
version: 0.1.0
description: MCP server for sttr CLI
runtime: python

install:
  - pip install mcp
  - curl -sfL https://raw.githubusercontent.com/abhimanyu003/sttr/main/install.sh | sh

start:
  command: python
  args: ["sttr.py"]

